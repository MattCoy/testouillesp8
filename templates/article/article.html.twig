{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block content %}

    <h1>{{ article.title }}</h1>
    <small> publié par {{ article.user.username }}php  le {{ article.datepubli|
        date('d/m/Y') }}</small>
    {% if article.image %}<img src="{{ asset('uploads/articles/images/' ~
        article.image) }}">{% endif %}
    <p>{{ article.content }}</p>

{% endblock %}
